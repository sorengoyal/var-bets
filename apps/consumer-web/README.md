# VARBET Consumer Web

Consumer-facing Next.js application for live VAR markets. This app is separate from `apps/api` and `apps/mock-service`.

```sh
pnpm --filter consumer-web dev
```

Copy `.env.example` to `.env.local` to configure the Nest API and Solana RPC connection.
