# pnpm-workspace.yaml
packages:
  - 'apps/*'
  - 'packages/*'

allowBuilds:
  '@scarf/scarf': false
  esbuild: true
  unrs-resolver: false

# Allow these dependencies to run build scripts
onlyBuiltDependencies:
  - 'esbuild'
  - 'unrs-resolver'
